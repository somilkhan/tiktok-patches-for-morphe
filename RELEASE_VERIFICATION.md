# Release verification

This marker intentionally triggers a semantic-release patch release so the current patch source is published as a Morphe `.mpp` bundle.

The Shipsy developer-options patch remains separately scoped under `app.morphe.patches.shipsy.developeroptions`.
